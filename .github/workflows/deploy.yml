name: Deployment
on:
  push:
    branches:
      - main
      - dev
env:
  MONGO_DB_NAME: gha-demo
jobs:
  test:
    env: 
      MONGODB_CLUSTER_ADDRESS: cluster0.wvxodlu.mongodb.net
      MONGONDB_USERNAME: ioannistzomiadis
      MONGONDB_PASSWORD: leJNHx4lBD96UXto
      PORT: 8080
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:$env:NAME
      - name: Run tests
        run: npm test
      - name: Output information
        run: echo "MONGODB_USERNAME: ${{ env.MONGODB_USERNAME }}"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "MONGODB_USERNAME: ${{ env.MONGODB_USERNAME }}"
          echo "MONGODB_DB_NAME: $env:MONGODB_DB_NAME"
          #  This is a test to prove that it will not work since the mongodb username is availabe only in the other job
          #  and only mongodb_db_name will output because it is available in the root workflow.